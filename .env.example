# Vite exposes vars prefixed with VITE_ to the browser
VITE_API_URL=

# ── Super Admin ────────────────────────────────────────────────────────────
# Set to your platform gym's MongoDB _id after first register.
# Must match server/.env SAAS_ADMIN_GYM_ID exactly.
VITE_SAAS_ADMIN_GYM_ID=
