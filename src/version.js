// Bump this with every shipped change (bug fix, feature, tweak) so the
// version shown on the Profile page's About section always identifies
// exactly which build a member is running — useful for confirming someone
// has picked up a fix.
//
// Format: MAJOR.MINOR.PATCH
//   - Small changes                 → bump MINOR (middle number)
//   - Minor updates / small fixes   → bump PATCH (last number)
//   - Major updates / big overhauls → bump MAJOR (first number)
export const APP_VERSION = '1.7.8'
