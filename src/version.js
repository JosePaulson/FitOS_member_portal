// Bump this with every shipped change (bug fix, feature, tweak) so the
// version shown on the Profile page always identifies exactly which build
// a member is running — useful for confirming someone has picked up a fix.
// Convention: semantic-ish MAJOR.MINOR.PATCH — PATCH for fixes/small
// tweaks, MINOR for new features, MAJOR for breaking/large overhauls.
export const APP_VERSION = '1.1.0'
